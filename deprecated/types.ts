export type MagicalObject = {
    code: string;
    name: string;
    description: string[];
};

export type HeroBase = {
    code: string;
    name: string;
    wikiUrl: string;
};

export type HeroBaseCode = string;
